import 'package:meta/meta.dart';

/// ViewModel
/// Contains mostly Strings and flags that the View uses to display the data.
/// Also, collections and even other ViewModels
@immutable
abstract class ViewModel {}
