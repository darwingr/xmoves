import 'package:test/test.dart';

import 'lib_thin_clean_blocs/bloc_usecase.dart' as domain;
import 'lib_thin_clean_blocs/panels.dart';

/// Builds on lib_clean_bloc_streams approach by considering:
///   if the bloc could be properly analogous to a classic UseCase object
///   if the flutter_bloc.BlocBuilder could be properly analogous to a
///     classic Controller object
/// The result would be the benefits of BLoCs used in 'Clean Architecture'
/// without any extra layers.
Future<void> main() async {
}
